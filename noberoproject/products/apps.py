
# This code defines the configuration for the `products` Django app.
# - The `ScraperConfig` class inherits from `AppConfig` and sets the `name` attribute to 'products', which refers to the name of the app.
from django.apps import AppConfig

class ScraperConfig(AppConfig):
    name = 'products'
